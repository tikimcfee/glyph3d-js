{
  "name": "glyph3d",
  "version": "0.1.0",
  "description": "glyph3d monorepo — @glyph3d/core library, r3f bindings, IDE app, and Go single-binary server",
  "type": "module",
  "private": true,
  "workspaces": [
    "packages/*",
    "keystone"
  ],
  "scripts": {
    "serve": "cd cli && ./glyph3d-cli serve --root .. --http 8000"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/yourusername/glyph3d-js.git"
  },
  "devDependencies": {
    "harfbuzzjs": "^0.4.8",
    "playwright": "^1.60.0",
    "three": "^0.183.0"
  }
}
